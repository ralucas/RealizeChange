extends ../layout

block content
	.container
		div.row-fluid.jumbotron#splash
			h2 #{routeName}
			if username
				p Welcome #{username}
				a.btn.btn-default(href='/logout') Log Out
			else
				a.btn.btn-default(href='/auth/google') Login with Google